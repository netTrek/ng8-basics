import { DangerBtnDirective } from './danger-btn.directive';

describe ( 'DangerBtnDirective', () => {
  it ( 'should create an instance', () => {
    const directive = new DangerBtnDirective ();
    expect ( directive )
      .toBeTruthy ();
  } );
} );
